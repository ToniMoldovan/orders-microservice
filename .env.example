# Application Port (mapped to nginx)
APP_PORT=8080

# PostgreSQL Database Configuration
# These match the POSTGRES_* environment variables in docker-compose.yml
POSTGRES_DB=orders
POSTGRES_USER=orders
POSTGRES_PASSWORD=orders
DB_PORT=5432
